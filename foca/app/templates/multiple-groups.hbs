{{#each this}}
<div class="card">
    <h5 class="card-header">{{name}}</h5>
    <div class="card-body">
        <p class="card-text">{{description}}</p>
        <a href="#groups/{{group_id}}" class="btn btn-primary">Details</a>
    </div>
</div>
<br>
{{/each}}